export const selectStyles = {
    control: (styles) => ({
        ...styles,
        padding: '7.5px 3px',
        borderRadius: '6px'
    }),
    indicatorSeparator: (styles) => ({
        ...styles,
        width: '0px'
    })
};
export const selectStylesSm = {
    control: (styles) => ({
        ...styles,
        padding: '0px 3px',
        borderRadius: '6px'
    }),
    indicatorSeparator: (styles) => ({
        ...styles,
        width: '0px'
    })
};
